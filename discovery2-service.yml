server:
  port: 8062

eureka:
  instance:
    hostname: localhost
  client:
    # registerWithEureka: false
    # fetchRegistry: false
    register-with-eureka: true #eureka registration
    fetch-registry: true  #replication
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8061/eureka/